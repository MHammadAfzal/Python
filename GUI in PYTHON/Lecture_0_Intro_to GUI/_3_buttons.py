# ! Creating buttons by using tkinter

# from tkinter import *

# root = Tk()
# creating a window layout

# def clicked():
# label = Label(root, text = "Hello, Hammad you clicked it")
# label.pack()
# defining a function which will be executed when the button will be clicked

# myButton = Button(root, text = "Click me", command = clicked, fg = "white", bg = "blue")
# creating a simple button

"""
: Button: A widget used to create a button

: param: root, text, command, fg, bg, padx, pady, state
: root: the button will go to the root
: text: it will appear on the button
: command: it requires a function which is gonna executed without parenthesis
: fg: foreground color in short text color for button
: bg: background color for button
: padx: padding for x-axis
: pady: padding for y-axis
: state: we can enable or disable the state of a button

"""


# myButton = Button(root, text = "Click me", padx = 50, pady = 50)
# creating a button widget with padding of x-axis and y-axis =  50 units

# myButton = Button(root, text = "Click me", state = DISABLED)
# creating a button widget with state disabled

# myButton.pack()
# # shoving onto the screen


# root.mainloop()

# TODO study creating input fields by using tkinter
