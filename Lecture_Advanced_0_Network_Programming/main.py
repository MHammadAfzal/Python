print("Hello, world")

# ! Network Programming in PYTHON

# * Write a web browser
# import socket

# sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)

# sock.connect(("data.pr4e.org", 80))

# cmd = "GET http://data.pr4e.org/romeo.txt HTTP/1.0\r\n\r\n".encode()
# sock.send(cmd)

# while True:
#     data = sock.recv(512)
#     if (len(data) < 1):
#         break
#     print(data.decode(), end='')

# sock.close()

# * Characters and Strings
# print(ord("H")) # Ascii code of H
# print(ord("A")) # Ascii code of A
# print(ord("a")) # Ascii code of a

# TODO 'urllib' library in PYTHON

# * Go to urllib.py to study about url library

# TODO Parsing HTML in PYTHON

# * Go to parseHtml.py to study about parsing html

